# Recoup backend — Python 3.12
# Pinned loosely for the buildathon; tighten before any real deployment.

fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6

# Persistence
sqlalchemy>=2.0
psycopg[binary]>=3.2        # Neon Postgres
alembic>=1.14

# Integrations
razorpay>=1.4
resend>=2.4
groq>=0.11                  # primary LLM provider (openai/gpt-oss-120b)
google-genai>=0.3           # zero-cost fallback provider
anthropic>=0.40             # alternative LLM provider (not default)

python-dotenv>=1.0
httpx>=0.27

# Dev
pytest>=8.3
pytest-asyncio>=0.24
ruff>=0.8
