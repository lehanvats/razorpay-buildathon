// Recovery rate per failure class, treatment vs control side by side.
//
// This is where the rail-aware-timing claim is evidenced: if salary-window
// scheduling works, SOFT_FUNDS should show the widest treatment-over-control
// gap of any class. If it doesn't, the chart says so — which is the point of
// measuring rather than asserting.

export function ClassBreakdown() {
  // TODO(step-08): grouped bars, one pair per FailureClass
  return null
}
