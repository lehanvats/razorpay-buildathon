# Copy to .env.local. Vite exposes VITE_* to the browser bundle, so NEVER put
# a secret here — anything in this file ships to every visitor.
# API keys belong in backend/.env only.

VITE_API_BASE_URL=/api
